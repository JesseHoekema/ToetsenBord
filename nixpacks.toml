[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["corepack enable", "corepack prepare pnpm@latest --activate", "pnpm install --no-strict-peer-dependencies"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run preview"

[env]
NODE_VERSION = "22.12.0"
